import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {},
  contentContainer: {},
  placeQuestionContainer: {
    justifyContent: 'center',
  },
});

export default styles;
